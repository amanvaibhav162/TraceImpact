{
  "name": "traceimpact-blockchain",
  "version": "1.0.0",
  "description": "Blockchain layer for TraceImpact transparency system",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.1"
  }
}